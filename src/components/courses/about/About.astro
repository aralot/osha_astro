---
import Icon from 'astro-icon';

import hero1 from './hero1.svg';
import hero2 from './hero2.svg';
import hero3 from './hero3.svg';
---

<div class="courses">
  <div class="title" id="courses">Учим программированию с нуля</div>
  <div class="description">
    Каждое направление подходит под возрастные особенности ребенка
  </div>
  <div class="grid">
    <div class="course">
      <img src={hero1} class="course-image" />
      <div class="age">8–9 лет</div>
      <div class="course-title">
        Визуальное программирование на&nbsp;Scratch
      </div>
      <div class="points">
        <div class="point">
          <Icon name="plus" />
          <div>Научимся составлять алгоритмы и писать скрипты</div>
        </div>

        <div class="point">
          <Icon name="plus" />
          <div>Создадим внешность героев, диалоги и анимацию</div>
        </div>
      </div>
    </div>

    <div class="course">
      <img src={hero2} class="course-image" />
      <div class="age">10–11 лет</div>
      <div class="course-title">Геймдизайн в среде Roblox Studio</div>
      <div class="points">
        <div class="point">
          <Icon name="plus" />
          <div>Познакомимся с интерфейсом и возможностями студии</div>
        </div>

        <div class="point">
          <Icon name="plus" />
          <div>Научимся работать в 2D и 3D и создадим игру-платформер</div>
        </div>
      </div>
    </div>
    <div class="course">
      <img src={hero3} class="course-image" />
      <div class="age">12–17 лет</div>
      <div class="course-title">Программирование на Python</div>
      <div class="points">
        <div class="point">
          <Icon name="plus" />
          <div>Познакомимся с&nbsp;переменными, типами данных и функциями</div>
        </div>

        <div class="point">
          <Icon name="plus" />
          <div>Разберемся в правилах языка и напишем первые строки кода</div>
        </div>
      </div>
    </div>
  </div>
  <div
    class="button"
    href="#form-enroll-1"
    onClick={event => {
      ym('reachGoal', 'click_button2');
      scrollToEnroll(event);
    }}
  >
    Записаться бесплатно
  </div>
</div>

<style lang="scss">
  .courses {
    padding: 48px 20px 60px;
    background: #f8f2ff;

    @media (min-width: 1200px) {
      border-radius: 32px;
      padding: 71px 58px 94px;
    }
  }

  .title {
    margin-bottom: 16px;

    font-weight: 700;
    font-size: 24px;
    line-height: 30px;
    color: var(--day-text-primary);

    @media (min-width: 1200px) {
      margin-bottom: 24px;

      font-size: 46px;
      line-height: 51px;
      letter-spacing: -0.02em;
    }
  }

  .description {
    max-width: 246px;
    margin-bottom: 32px;

    font-size: 16px;
    line-height: 20px;
    color: var(--day-text-primary);

    @media (min-width: 1200px) {
      max-width: unset;
      margin-bottom: 44px;
      font-size: 22px;
      line-height: 29px;
      letter-spacing: -0.01em;
    }
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-column-gap: 10px;
    overflow-x: scroll;
    width: calc(100% + 40px);
    margin: 0 -20px 32px;
    padding: 0 20px;

    &::-webkit-scrollbar {
      height: 0;
    }

    @media (min-width: 1200px) {
      width: unset;
      overflow-x: unset;
      grid-column-gap: 20px;
      margin: 0 0 40px;
      padding: unset;
    }
  }

  .age {
    margin-bottom: 8px;
    position: relative;

    font-size: 13px;
    font-weight: bold;
    line-height: 16px;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: var(--day-text-primary);

    @media (min-width: 1200px) {
      font-size: 14px;
      line-height: 18px;
    }
  }

  .course-title {
    margin-bottom: 16px;
    position: relative;

    font-weight: 700;
    font-size: 18px;
    line-height: 24px;

    letter-spacing: -0.02em;
    color: var(--day-text-primary);

    @media (min-width: 1200px) {
      font-size: 22px;
      line-height: 29px;
    }
  }

  .points {
    display: grid;
    grid-row-gap: 12px;
    position: relative;
  }

  .point {
    display: grid;
    grid-template-columns: 16px auto;
    grid-column-gap: 8px;

    font-size: 15px;
    line-height: 19px;
    letter-spacing: -0.01em;

    color: var(--day-text-primary);

    svg {
      margin-top: 2px;
    }

    @media (min-width: 1200px) {
      font-size: 16px;
      line-height: 21px;
    }
  }

  .course {
    height: 430px;
    width: 253px;
    padding: 24px 20px;
    position: relative;

    background: white;
    background-repeat: no-repeat;
    background-position: bottom right;
    background-size: contain;
    border-radius: 24px;

    &:nth-of-type(1) {
      background-image: url('${hero1}');

      .point > svg {
        color: #4ea9fe;
      }
    }

    &:nth-of-type(2) {
      background-image: url('${hero2}');

      .point > svg {
        color: #833ae0;
      }
    }

    &:nth-of-type(3) {
      background-image: url('${hero3}');

      .point > svg {
        color: #2cbb45;
      }
    }

    @media (min-width: 1200px) {
      width: unset;
      height: 482px;
      padding: 32px;
    }
  }

  .course-image {
    position: absolute;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    display: block;
    object-position: bottom right;
    object-fit: contain;
  }

  .button {
    display: block;
    padding: 12px 0;
    font-size: 18px;
    line-height: 24px;
    text-align: center;
    color: white;
    background: #2cbb45;
    border-radius: 12px;
    cursor: pointer;
    outline: none;
  }
</style>
